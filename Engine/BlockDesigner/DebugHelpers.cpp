#include "pch.h"
#include "DebugHelpers.h"


void DebugMsg(PCWSTR _format, ...) 
{
    //va_list args;
    //va_start(args, _format);

    //WCHAR msg[MAX_PATH];

    //if (SUCCEEDED(StringCbVPrintfW(msg, sizeof(msg), _format, args))) {
    //    OutputDebugStringW(msg);
    //}
}